boto >= 2.49.0
boto3 >= 1.9.203
ansible<=12.0.0
PyMySQL >= 0.9.3
# ansible[azure] # Does not work from file. Need to be installed manually
urllib3>=2.6.0 # not directly required, pinned by Snyk to avoid a vulnerability